import React from 'react';

const LoginLayout = ({
	children,
}: Readonly<{
	children?: React.ReactNode;
}>) => {
	return (
		<div>
			ahihiih
			{children}
		</div>
	);
};

export default LoginLayout;
